---
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'outline' | 'outline-white';
  size?: 'sm' | 'md' | 'lg';
  class?: string;
  external?: boolean;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  class: className = '',
  external = false,
} = Astro.props;

const base = import.meta.env.BASE_URL;
const resolvedHref = href && !external && !href.startsWith('http') ? `${base}${href}` : href;

const variantClasses = {
  primary: 'bg-olive text-white hover:bg-olive-hover',
  secondary: 'bg-bg-dark text-white hover:bg-primary-light',
  outline: 'border border-primary text-primary hover:bg-primary hover:text-white',
  'outline-white': 'border border-white text-white hover:bg-white hover:text-primary',
};

const sizeClasses = {
  sm: 'px-5 py-2 text-xs',
  md: 'px-7 py-3 text-sm',
  lg: 'px-9 py-4 text-base',
};
---

{href ? (
  <a
    href={resolvedHref}
    target={external ? '_blank' : undefined}
    rel={external ? 'noopener noreferrer' : undefined}
    class:list={[
      'inline-block rounded-full font-medium tracking-[0.05em] transition-all duration-300 hover:scale-[1.02] active:scale-[0.98]',
      variantClasses[variant],
      sizeClasses[size],
      className,
    ]}
  >
    <slot />
  </a>
) : (
  <button
    class:list={[
      'inline-block rounded-full font-medium tracking-[0.05em] transition-all duration-300 hover:scale-[1.02] active:scale-[0.98]',
      variantClasses[variant],
      sizeClasses[size],
      className,
    ]}
  >
    <slot />
  </button>
)}
